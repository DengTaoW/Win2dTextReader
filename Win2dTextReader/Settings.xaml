<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Win2dTextReader.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win2dTextReader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border Padding="20" 
            CornerRadius="{StaticResource OverlayCornerRadius}" 
            BorderThickness="1"
            BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
            Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}">

        <Grid RowSpacing="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Text="设置" />

            <Grid RowSpacing="10" ColumnSpacing="10" Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Row="0" Grid.Column="0"
                          Header="字体大小"
                          ItemsSource="{x:Bind ViewModel.FontSizeItems}"
                          HorizontalAlignment="Stretch"
                          SelectedIndex="{x:Bind ViewModel.FontSizeIndex, Mode=TwoWay}" >
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="local:DoubleItem">
                            <TextBlock Text="{x:Bind DisplayText}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <ComboBox Grid.Row="0" Grid.Column="1"
                          Header="字体名称"
                          HorizontalAlignment="Stretch"
                          ItemsSource="{x:Bind ViewModel.FontItems}"
                          SelectedIndex="{x:Bind ViewModel.FontFamilyIndex, Mode=TwoWay}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="local:FontItem">
                            <TextBlock Text="{x:Bind DisplayFontName}" FontFamily="{x:Bind FontFamily}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <ComboBox Grid.Row="1" Grid.Column="0"
                          Header="行高"
                          HorizontalAlignment="Stretch"
                          ItemsSource="{x:Bind ViewModel.LineHeightItems}"
                          SelectedIndex="{x:Bind ViewModel.LineHeightIndex, Mode=TwoWay}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="local:DoubleItem">
                            <TextBlock Text="{x:Bind DisplayText}" /> 
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <ComboBox Grid.Row="1" Grid.Column="1"
                          Header="阅读主题"
                          HorizontalAlignment="Stretch"
                          ItemsSource="{x:Bind ViewModel.ThemeItems}"
                          SelectedIndex="{x:Bind ViewModel.ThemeIndex, Mode=TwoWay}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="local:AppTheme">
                            <Grid Background="{x:Bind Background}" >
                                <TextBlock Foreground="{x:Bind Foreground}" Text="{x:Bind Name}" /> 
                            </Grid>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                
            </Grid>
        </Grid>
    </Border>
</UserControl>
