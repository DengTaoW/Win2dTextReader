<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Win2dTextReader.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win2dTextReader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border Padding="20" 
            CornerRadius="{StaticResource OverlayCornerRadius}" 
            BorderThickness="1"
            BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
            Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}">

        <Grid RowSpacing="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" >
                设置
            </TextBlock>

            <Grid RowSpacing="10" ColumnSpacing="10" Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="FontSizeComboBox"
                          Grid.Row="0" Grid.Column="0"
                          Header="字体大小"
                          HorizontalAlignment="Stretch"
                          ItemsSource="{x:Bind FontSizeValues}"
                          SelectionChanged="FontSizeComboBox_SelectionChanged">
                </ComboBox>

                <ComboBox x:Name="FontFamilyComboBox"
                          Grid.Row="0" Grid.Column="1"
                          Header="字体名称"
                          HorizontalAlignment="Stretch"
                          ItemsSource="{x:Bind FontFamilyValues}"
                          SelectionChanged="FontFamilyComboBox_SelectionChanged">
                </ComboBox>

                <ComboBox x:Name="LineHeightComboBox"
                          Grid.Row="1" Grid.Column="0"
                          Header="行高"
                          HorizontalAlignment="Stretch"
                          ItemsSource="{x:Bind LineHeightValues}"
                          SelectionChanged="LineHeightComboBox_SelectionChanged">
                </ComboBox>
            </Grid>
        </Grid>
    </Border>
</UserControl>
